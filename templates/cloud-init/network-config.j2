version: 1
config:
  - type: physical
    name: eth0
    subnets:
      - type: static
        address: {{ ip }}
        netmask: {{ netmask }}
        gateway: {{ gateway }}
        dns_nameservers:
          - {{ nameserver }}
